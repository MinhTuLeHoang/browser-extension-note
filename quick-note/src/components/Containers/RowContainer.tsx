import styled from 'styled-components';
import { ICustomDiv } from './Container';

const RowContainer = styled.div<ICustomDiv>`
	display: flex;
	flex-direction: row;
	align-items: center;
`;

export default RowContainer;
